# TEMPLATE — fill in real values, do NOT commit them.
# FERNET_KEY: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# WEB_SECRET_KEY: any long random string
# POSTGRES_PASSWORD: the Airflow metadata DB password
apiVersion: v1
kind: Secret
metadata:
  name: dpn-airflow-secrets
  namespace: ns-dpn-01
type: Opaque
stringData:
  FERNET_KEY: "<fernet-key>"
  WEB_SECRET_KEY: "<web-secret>"
  POSTGRES_PASSWORD: "<postgres-password>"
